# Test-only deps. At runtime fastapi + langchain-core are provided by the host
# (protoAgent); the PTY backend is pure stdlib. These let the suite run standalone
# in CI with no host.
fastapi>=0.110
langchain-core>=0.2
pyyaml>=6
httpx>=0.27
pytest>=8
pytest-asyncio>=0.23
# wsproto powers fastapi/starlette TestClient.websocket_connect (used by the WS test).
wsproto>=1.2
